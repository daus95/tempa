FIRST STEP — MUST BE DONE BEFORE WRITING ANY CODE:
Re-read the ${epic} specification file from the folder: ${sources.epics}

This is a continuation of the ${epic} implementation. Continue or fix the following 🔧 and ⬜ feature list, in order.

Application code (monorepo, MANY services): ${sources.apps}
  → Determine the relevant service/folder based on the epic specification. Each service's source code AND tests live inside its own service folder (e.g. apps/backend, apps/web, etc.).
New specification (PRD)      : ${sources.prd}
Current system documentation : ${sources.docs}
