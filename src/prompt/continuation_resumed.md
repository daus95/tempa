This is a continuation of the SAME ${epic} implementation session — you already read the ${epic} specification file (from ${sources.epics}) and the relevant application code/PRD/docs earlier in this conversation, so do NOT re-read them from scratch. Only re-read a specific file if you're genuinely unsure it still matches what you remember (e.g. checking that an edit you made actually saved).

Continue or fix the following 🔧 and ⬜ feature list, in order.

Application code (monorepo, MANY services): ${sources.apps}
  → Determine the relevant service/folder based on the epic specification. Each service's source code AND tests live inside its own service folder (e.g. src/backend, src/web, etc.).
New specification (PRD)      : ${sources.prd}
Current system documentation : ${sources.docs}
